# 校园跑腿全栈项目 - 交付测试报告

**交付日期**: 2026-09-16
**项目位置**: `D:\Desktop\SooH`

---

## 一、项目概述

校园跑腿微信小程序全栈项目，包含三端：

| 端 | 技术栈 | 端口 | 页面数 |
|---|---|---|---|
| 微信小程序 | 微信原生框架 | - | 15 |
| Web管理后台 | Vue 3 + Vite + Element Plus + Pinia + ECharts | 5173 | 14 |
| 后端服务 | Node.js + Express + MySQL + JWT | 3000 | 10个路由模块 |

---

## 二、本次完成的工作

### 2.1 后端API完善（server/）

**新增数据库表**:
- `pricing_configs` - 定价配置表（5条初始数据）
- `system_settings` - 系统设置表（6项初始设置）

**修复/完善的接口**:

| 模块 | 接口 | 说明 |
|---|---|---|
| 认证 | `POST /api/auth/wx-login` | 修复mock，根据code生成openid，查询或创建用户，自动建钱包 |
| 认证 | `POST /api/auth/phone-login` | 新用户创建时事务内同时初始化钱包 |
| 认证 | `POST /api/auth/admin-login` | 增加管理员状态校验，更新last_login_at |
| 看板 | `GET /api/admin/order-trend` | 新增，近N天订单量+交易额趋势 |
| 看板 | `GET /api/admin/service-type-stats` | 新增，服务类型订单占比 |
| 看板 | `GET /api/admin/info` | 新增，当前管理员信息 |
| 看板 | `GET /api/admin/orders/:id` | 新增，管理员订单详情（含轨迹） |
| 系统 | `GET/PUT /api/admin/system-settings` | 新增，系统设置读写 |
| 定价 | `GET/POST/PUT/DELETE /api/pricing` | 新增，定价配置CRUD |
| 优惠券 | `PUT/DELETE /api/coupons/:id` | 补全，更新/删除优惠券 |
| 优惠券 | `PUT /api/coupons/:id/status` | 新增，上下架优惠券 |
| 公告 | `PUT/DELETE /api/notices/:id` | 补全，更新/删除公告 |
| 校区 | `PUT/DELETE /api/campus/:id` | 补全，更新/删除校区 |
| 订单 | 全状态轨迹 | 新增，创建/接单/取货/送达/确认/取消均写入order_tracks |
| 订单 | `POST /api/orders/:id/rate` | 修复，增加订单状态/归属/重复评价校验 |
| 订单 | `POST /api/orders/:id/complaint` | 新增，用户提交投诉 |
| 订单 | `POST /api/orders/:id/confirm` | 修复，增加订单存在/归属/状态校验 |
| 跑腿员 | `POST /api/riders/orders/:id/deliver` | 修复，送达后状态保持delivering，钱包入账+流水记录 |
| 跑腿员 | `PUT /api/riders/online` | 新增，切换在线状态 |
| 跑腿员 | `POST /api/riders/withdraw` | 修复，事务内扣余额+写流水+创建提现单 |
| 跑腿员 | riderAuth中间件 | 修复，接单大厅/抢单/取货/送达/收入/提现加riderAuth |

**业务逻辑修复**:
- 送达后订单状态为 `delivering`（等待用户确认收货），而非直接 `completed`
- 跑腿员送达时自动增加钱包余额、写入收入流水、更新累计订单数和收入
- 新用户注册自动创建钱包记录

### 2.2 Vue管理后台去Mock（admin/）

**删除的mock数据**:
- Dashboard.vue: `useMockData()` 函数（45行硬编码数据，含统计卡片、趋势图、饼图、最新订单）
- Orders.vue: `mockOrders` 数组（5条假订单）
- Users.vue: `mockUsers` 数组（8条假用户）+ "mock模式直接更新"逻辑
- Riders.vue: `mockRiders` 数组（6条假跑腿员）
- RiderAudit.vue: `mockData` 数组（4条假申请）
- Finance.vue: `mockWithdrawals` + `mockTransactions` 数组
- Complaints.vue: setTimeout模拟加载 + `mockData` 数组
- Pricing.vue: 硬编码价格列表（5条）
- Campus.vue: setTimeout模拟 + 硬编码校区数据（2条）
- Coupons.vue: setTimeout模拟 + 硬编码优惠券（4条）
- Notices.vue: setTimeout模拟 + 硬编码公告（4条）
- System.vue: 硬编码系统设置 + 管理员列表

**新建API模块**（src/api/）:
- `complaints.js` - 投诉列表、处理投诉
- `pricing.js` - 定价CRUD
- `campus.js` - 校区CRUD
- `coupons.js` - 优惠券CRUD+状态切换
- `notices.js` - 公告CRUD
- `system.js` - 系统设置读写

**14个页面全部对接真实API**，API失败时由request.js拦截器统一显示错误提示，数据区域保持空状态，绝不显示假数据。

### 2.3 微信小程序去Mock（User/）

**删除的mock数据**:
- login.js: `mockUser`/`mockToken` 演示模式登录兜底
- order-list.js: `mockOrders` 数组（3条假订单）
- index.js: 硬编码公告、校区列表、热门服务（改为从API获取）
- mine.js: 硬编码余额12.50、优惠券3张、积分268
- wallet.wxml: 写死的余额"12.50"改为数据绑定
- admin-mobile.js: `admin_mock_token` 硬编码
- 所有页面: API失败时的mock回退逻辑全部删除

**15个页面全部对接真实API**:

| 页面 | 对接的API |
|---|---|
| index | `/campus`、`/notices`、`/pricing` |
| login | `/auth/phone-login`、`/auth/send-code`、`/auth/wx-login` |
| order-express | `/pricing`、`/users/addresses`、`/orders` |
| order-canteen | `/pricing`、`/users/addresses`、`/orders` |
| order-list | `/orders/my`、`/orders/:id/confirm`、`/orders/:id/cancel`、`/orders/:id/rate` |
| order-detail | `/orders/:id`（含轨迹tracks） |
| mine | `/users/profile`、`/users/wallet`、`/users/coupons`、`/riders/apply` |
| wallet | `/users/wallet` |
| coupon | `/users/coupons` |
| address | `/users/addresses` CRUD |
| message | `/orders/my`、`/notices` |
| rider-hall | `/riders/hall`、`/riders/grab/:id`、`/riders/online`、`/riders/income` |
| rider-order | `/riders/orders`、`/riders/orders/:id/pickup`、`/riders/orders/:id/deliver` |
| rider-mine | `/riders/profile`、`/riders/income`、`/riders/withdraw` |
| admin-mobile | `/auth/admin-login`、`/admin/dashboard` |

---

## 三、端到端业务流程测试结果

**测试脚本**: `D:\Desktop\SooH\e2e_test.js`
**测试时间**: 2026-09-16

| 步骤 | 操作 | 结果 |
|---|---|---|
| 1 | 用户手机号登录（13900004444 / 123456） | ✅ 成功，自动创建钱包 |
| 2 | 用户下单（快递代取，金额5元+小费1元） | ✅ 成功，订单ID=5 |
| 3 | 管理员登录（admin / admin123） | ✅ 成功 |
| 4 | 跑腿员入驻申请（13900005555） | ✅ 成功 |
| 5 | 管理员审核通过跑腿员 | ✅ 成功，用户角色变为rider |
| 6 | 跑腿员抢单 | ✅ 成功，订单状态picked |
| 7 | 跑腿员确认取货 | ✅ 成功，订单状态delivering |
| 8 | 跑腿员确认送达 | ✅ 成功，钱包入账6元，流水记录 |
| 9 | 用户确认收货 | ✅ 成功，订单状态completed |
| 10 | 用户评价（5星） | ✅ 成功 |
| 11 | 订单详情查询 | ✅ 5条轨迹完整：创建→接单→取货→送达→确认收货 |
| 12 | 跑腿员收入统计 | ✅ 今日收入5元，本月1单，余额6元 |

**管理后台API验证**:
- 数据看板: 今日订单5，交易额15元，新增用户10，活跃跑腿员2 ✅
- 订单趋势: 7天数据 ✅
- 服务类型占比: 正常返回 ✅
- 订单列表: 5条真实订单 ✅
- 用户列表: 11个真实用户 ✅
- 定价配置: 5条 ✅
- 系统设置: 正常读取 ✅

**Mock数据清理验证**:
- Vue管理后台: `mock|模拟数据|假数据|演示模式` 关键词搜索 **0匹配** ✅
- 小程序端: `mock|模拟数据|假数据|演示模式|admin_mock` 关键词搜索 **0匹配** ✅

---

## 四、启动说明

### 4.1 环境要求
- Node.js >= 14
- MySQL >= 5.7（数据库已初始化，root/123456）
- 微信开发者工具（小程序端测试）

### 4.2 启动后端
```bash
cd D:\Desktop\SooH\server
node app.js
# 服务运行在 http://localhost:3000
```

### 4.3 启动管理后台
```bash
cd D:\Desktop\SooH\admin
npm run dev
# 服务运行在 http://localhost:5173
# 默认管理员账号: admin / admin123
```

### 4.4 启动小程序
1. 用微信开发者工具打开 `D:\Desktop\SooH\User` 目录
2. 在详情→本地设置中勾选 **"不校验合法域名、web-view（业务域名）、TLS版本以及HTTPS证书"**
3. 编译运行
4. 测试手机号登录：任意手机号 + 验证码 `123456`

### 4.5 跑腿员测试流程
1. 用户端登录后，在"我的"页面点击"跑腿员入驻"提交申请
2. 管理后台登录后，在"跑腿员审核"页面审核通过
3. 小程序端重新登录，角色自动变为跑腿员
4. 底部tab切换到跑腿员模式，可查看接单大厅、抢单、配送

---

## 五、测试账号

| 角色 | 账号 | 密码/验证码 | 说明 |
|---|---|---|---|
| 管理员 | admin | admin123 | Web管理后台 |
| 普通用户 | 任意手机号 | 123456 | 小程序端，自动注册 |
| 跑腿员 | 入驻申请后管理员审核 | - | 需先申请再审核 |

---

## 六、已知说明

1. **微信登录**: `wx-login` 接口使用模拟openid生成（未接入真实微信API），手机号登录可正常使用
2. **短信验证码**: 测试环境固定验证码 `123456`，未接入真实短信服务
3. **支付功能**: 项目采用货到付款模式，未接入微信支付，钱包余额仅用于跑腿员收入和提现
4. **小程序端**: 需在微信开发者工具中测试，确保勾选"不校验合法域名"
5. **跑腿员接口**: 带 `riderAuth` 中间件，需管理员审核通过后才能访问
6. **e2e_test.js**: 项目根目录下的端到端测试脚本，可用于回归测试 `node e2e_test.js`

---

## 七、文件结构

```
D:\Desktop\SooH\
├── User/                    # 微信小程序（15个页面）
├── admin/                   # Vue管理后台（14个页面）
│   └── src/
│       ├── api/             # 12个API模块
│       ├── views/           # 14个页面组件
│       ├── stores/          # Pinia状态管理
│       └── utils/           # 请求封装
├── server/                  # Node.js后端
│   ├── controllers/         # 5个控制器
│   ├── routes/              # 10个路由模块
│   ├── middleware/          # JWT认证中间件
│   └── config/              # 数据库配置
├── database/
│   └── init.sql             # 数据库初始化脚本（17张表）
└── e2e_test.js              # 端到端测试脚本
```
